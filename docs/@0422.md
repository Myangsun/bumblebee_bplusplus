Task list:
Use subagents to research and write or analyze for each task, remember this is a graduate AI thesis paper project.

1. still waiting for d5/d6 gpus running results.
2. Change the title to True absence or detection failure: Generative Augmentation and Expert-Calibrated Filtering for Long-Tailed Bumblebee Classification. Does it make sense?
And at the end of writing, refine the title and abstract.
3. Use a consistent color for the 3 species across plots
4. always use B. ashtoni; B. sandersoni ; B. flavidus; as names
5. I am not happy with sections 5 and 6. There are still bugs and inconsistent statements. I have detailed suggestions and questions: 

a.5.1 baseline, since baseline results have been updated from when /home/msun14/bumblebee_bplusplus/docs/plots/baseline_f1_ci.png was created. So I need to update this plot, and I also want to include species metrics and confusion plots in the paper.
b. 5.3/5.4 llm and expert filter analysis should use similar plots in terms of content and style: include pass/filter tunnel; blind ID analysis; per feature score analysis; failure mode analysis; contact images of pass and fail
c. 5.4.4 Filter comparison on the 1,500-image pool: What do these plots mean? How to interpret this: "er-synthetic centroid cosine × probe pass-probability on the 1,500-image pool, coloured by LLM-strict, shape-marked by expert-strict where labeled. Dashed lines mark per-species τ for each filter.
"
d.[Figure 5.15: plots/filters/expert_coverage_of_selected200.png] Expert-label composition of each filter’s 200-image selection. Green: expert-strict pass; orange: lenient-only; red: expert fail; grey: unlabelled. D6 probe selects the largest expert-strict subset across all three species within the same 200-image cap.
This kind of measure probe performance should move from 5.5.5 to 5.4.3, and do I need to include other analysis of probe accuracy?
e.5.5  add a 5.5.1 just showing single run results plots across species in d1-d6 and a table. This is a short table, single run + CI, and 5.5.2 as a full wide table.

6. A lot of changes I want to make to 5.6, and also impact section 6
a. I do not like the word Mechanistic Analysis, could it be changed to results, or failure reason insights analysis? Or change it to a datapoint/image-level analysis. Or quantitative analysis? 
b. I do not understand the relationship between 5.6.1 Per-image prediction flips; 5.6.2 Embedding-space failure chains; 5.6.3 Judge–classifier disagreement; 5.6.4 Causal attribution via subset ablation. I think Judge–classifier disagreement is useless. Maybe just plot images that pass/fail llm/latent/expert filters? and for causal attribution. One important thing I get from looking at images is that sandersoni is better generated. Maybe add one species to the baseline to see if it could improve performance.  Rather than take one out

7. section 6 6.1-6.5
a. 6.1 Augmentation harm is concentrated in the rare tier 
to 6.1 the Synthetic–Real Gap for Fine-Grained Biodiversity
Previous 6.1 has the wrong interpretation; this study only augments rare species, and it is expected that other species are not impacted. This is not a finding.
synthetic–real gap is not uniform across species: several potential reasons (needs to add evidence in the experiments): prior knowledge in LLM; some species are more difficult to identify than others, quoting from a biologist and also a field guide. Sanderson is just easier to identify. Flavius actually has a lot of messy banding variants in the field ID. morphology-specific gap

b.6.2 A mechanistic account of synthetic-augmentation harm 
to 6.2 Why Synthetic Augmentation Degrades Performance 
I want to say that synthetic images without quality filtering is actually harm to the performance. It is proved by the d3 synthetic images only.
Both qualitative and quantitative analysis findings: Synthetic embedding/ real images clusters; Single species added results/ Image level analysis?

c. Change 6.4 to LLM judges vs Expert judges. 
From the image contact grid, there are differences between the LLM pass/fail and the expert pass/fail.
Also, analyses are based on both metrics and qualitative image-level assessments.

d. I do not like writing like: "The remaining residual is power. With df = 4, paired t-tests demand large effect sizes for 80 % power at α = 0.05; non-significant comparisons such as D1 vs. D2 (p = 0.161) and D1 vs. D3 (p = 0.096) are underpowered rather than null. Bootstrap confidence intervals — reported alongside every point estimate — are the more informative uncertainty quantification for rare-species F1, and the prose in Section 5 follows that priority." It is so confusing about what it is trying to convey.
Maybe something like: "Our significance tests have too few data points to detect small real differences, so when a comparison fails significance, it likely means we couldn't measure the gap, not that there is no gap — so we report confidence intervals (which show the range of plausible effects) rather than relying on p-values." is better

Before actual rewrite the thesis, plan using agents to do research and answer questions until refinements are clear. you could analyze the key results and key finding first for this thesis, present to me and then build on this. 

scripts/make_expert_filter_plots.py: Okabe-Ito palette (#0072B2 / #E69F00 / #009E73), SHORT map now "B. ashtoni" etc., filename-safe FSLUG for grid PNGs. Regenerated 22 figures under docs/plots/filters/ + docs/plots/filters/grids/

a2	scripts/plot_150_outcomes.py + scripts/plot_embeddings.py (used by plot_embedding_atlas.py): same Okabe-Ito palette for the 3 rare species. Figures not regenerated — you may want to preview visually first; run python scripts/plot_150_outcomes.py and python scripts/plot_embeddings.py when ready

scripts/plot_baseline.py created (vectorised bootstrap, Okabe-Ito palette, 3 outputs)
docs/plots/baseline_f1_ci.png regenerated (F1 + 10k-iter bootstrap CI, 16 species sorted by test support, rare highlighted)
docs/plots/baseline_species_metrics.png new (precision / recall / F1 bars, rare highlighted)
docs/plots/baseline_confusion_matrix.png regenerated (row-normalised, rare rows+cols highlighted, rare-species-first sort)
§5.1 prose updated: new paragraph referencing Figure 5.1a + Figure 5.1b; confusion matrix renumbered to Figure 5.1c
Fixed long-standing §5.2.4 / §5.4 figure number collision via +1 cascade shift: current figure map is now §5.1→5.1a/b/c, §5.2→5.2–5.4, §5.4→5.5–5.18 (old 5.4a–5.17), §5.5→5.19–5.20 (old 5.18–5.19), §5.6→5.21–5.24 (old 5.20–5.23)
Verified §6 cross-refs remain valid after shift

source venv/bin/activate && python scripts/plot_single_run_species.py 2>&1 | tail -3

# Group 1 — parallel, no deps (evaluate what's already trained at wrong ckpt)
sbatch jobs/evaluate_f1_d5_d6.sh                       # eval 20 D5/D6 base models
sbatch jobs/evaluate_f1_subset_ablation.sh             # eval 6 D3/D4 subtractive

# Group 2 — parallel, no deps (start training the remaining ablations)
sbatch jobs/train_additive_ablation.sh                 # 3 additive runs (D3 pool)
sbatch jobs/train_subset_ablation_d5_d6.sh             # 6 D5/D6 subtractive runs

# Group 3 — submit ONLY after matching Group 2 job completes
sbatch jobs/evaluate_f1_additive_ablation.sh           # after train_additive_ablation.sh
sbatch jobs/evaluate_f1_subset_ablation_d5_d6.sh       # after train_subset_ablation_d5_d6.sh
On