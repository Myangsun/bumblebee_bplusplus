The raw numbers (macro F1, directly from @f1 JSONs)
Protocol	D1	D2 CNP	D3	D4	D5	D6	Winner
Seed-42 single	0.843	0.845	0.830	0.839	0.824	0.840	D2
Multi-seed (5 seeds mean)	0.839	0.822	0.828	0.831	0.827	0.834	D1
5-fold CV (5 folds mean)	0.832	0.837	0.820	0.821	0.824	0.824	D2
The per-seed reality — D1 > D6 on 4 of 5 seeds
Multi-seed per-seed D1 macro F1: 42→0.843, 43→0.842, 44→0.831, 45→0.834, 46→0.844.
Multi-seed per-seed D6 macro F1: 42→0.840, 43→0.841, 44→0.824, 45→0.826, 46→0.837.
D1 beats D6 on seeds 42, 44, 45, 46. D6 beats D1 only on seed 43. This isn't an artifact of averaging — it's consistent.

Why this actually makes sense (it's not a bug)
Only 3 of 16 species are augmented. Macro F1 averages over 16 species. The 13 non-augmented species get zero benefit, and can get slight harm from class-balance shift when synthetic images dilute the gradient signal. So macro F1 is a pessimistic yardstick for augmentation — by construction, the maximum possible macro F1 lift is ≈3/16 × (per-species F1 lift), i.e. ~0.02 even for a +0.10 per-species gain.

Rare-tier test sets are 6 / 10 / 36 images. One flipped test image moves rare-tier F1 by 0.05–0.17. D1-vs-D6 macro gap of 0.005 is deep inside the noise floor (5-fold paired-t, df=4, p=0.25).

The synthetic images genuinely occupy a different BioCLIP region from real images of the same species (Section 5.2, t-SNE + centroid distance). The classifier learns features tuned to the synthetic cluster that the real test images don't fall into. This is directly measured in the thesis, not speculation.

The LLM judge is near-random at ranking expert quality (AUC 0.56). So LLM-filtered D4 doesn't fix D3. This is also measured.

But augmentation DOES win — just not on macro F1
The real wins are per-species and per-test-image:

D2 CNP +0.059 on B. flavidus, p=0.005 — the ONLY statistically significant per-species gain in the study (5-fold paired t).
D6 is the only variant with 0 harmed B. ashtoni and 0 harmed B. sandersoni under multi-seed majority vote (Table 5.9).
D6 is the only variant where removing synthetics LOSES F1 on all three rare species (Table 5.7 subtractive ablation: −0.121 / −0.056 / −0.025).
D1 + B. sandersoni-only synthetics → macro F1 0.857 (highest single-run in study), with +0.218 boost to B. ashtoni (Table 5.8 additive — cross-species transfer).
What this means for your thesis
The finding is not "augmentation doesn't work." The finding is:

Generative augmentation at this scale has subtle, per-species effects that macro F1 averages out.
Simple copy-paste augmentation (D2 CNP) wins on 5-fold for one rare species (B. flavidus, significantly).
Expert-probe filtering (D6) is the only generative variant that is causally helpful for every rare species and harms no rare-species test image for 2 of 3.
Cross-species transfer (D1+sandersoni) is the biggest lever — larger than any own-species augmentation.

The story now reads cleanly: "At the single-split reading, D6 wins by +0.025 with every filtered variant beating baseline; the multi-seed and 5-fold tightenings show D6 remains numerically best among generative filters but the macro-F1 lever is bounded because only 3 of 16 species are augmented; the real evidence is the per-species causal ablation (Table 5.7/5.8/5.9) where D6 is the only variant with all-non-harmful cells and zero harmed rare-species test images."


To launch

cd /home/msun14/bumblebee_bplusplus

ASM=$(sbatch --parsable jobs/volume_ablation_assemble.sh)
TRN=$(sbatch --parsable --dependency=afterok:$ASM jobs/volume_ablation_train.sh)
EVL=$(sbatch --parsable --dependency=afterok:$TRN jobs/volume_ablation_evaluate.sh)
echo "assemble=$ASM  train=$TRN  eval=$EVL"
After eval finishes:


python scripts/plot_volume_ablation.py          # → docs/plots/volume_ablation_trends.{png,pdf}
python scripts/dump_final_metrics.py            # optional: regenerate final_metrics.md

only one left concern is the analysis of llm filters. what could you get from llm as judge? one thing I notice when I comparing llm pass and fail contacts and expert filter pass and fail contacts I found there are some quantitive pattern. Did the research notice or report on this matter?

The current rubric fuses two questions: "is this the right species?" and "does this image show enough of the species?" Split them:

plots errors:
1. this text overlap with plot: /home/msun14/bumblebee_bplusplus/docs/plots/filters/llm_vs_expert_strict_2x2.pdf
2. this I do not want to show common/rare differences, maybe just use 3 different colors for 3 metrics: /home/msun14/bumblebee_bplusplus/docs/plots/baseline_species_metrics.pdf
3. /home/msun14/bumblebee_bplusplus/docs/plots/single_run_species_f1.pdf: if you are using shades of colors showing d1 -> d6 make it consistent from lighter to darker

what does this plot mean? /home/msun14/bumblebee_bplusplus/docs/plots/filters/probe_calibration_reliability.pdf
show the dots completely

how about just re-run llm as judge v1 again? I feel it is not very stable. if running it again, will the labels change a lot?

Before integrating v1→v2 findings into the thesis, flagging the 2 FAILs the extended verifier caught (they are real — Table 5.4's F1 numbers are internally consistent with its own TP/FP/FN/TN cells, but those cells use τ with recall=1 rather than the JSON's F1-max τ):

thesis line 545 / Table 5.4: sandersoni F1 = 0.900 vs JSON 0.871; flavidus F1 = 0.500 vs JSON 0.320
thesis line 333 correctly cites the JSON values (0.87, 0.32) — so the thesis is internally inconsistent between §4.4.4 and §5.4.3.
You can resolve later by either clarifying "Table 5.4's F1 is at τ chosen for recall=1; JSON's F1 is at τ chosen to maximise F1 without recall constraint" or regenerating one to match the other. I'll leave that to you.