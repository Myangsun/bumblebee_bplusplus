  the literature flags 5:1+ as risky for ResNet-50 due to texture bias??
  
  ## Literature sources for partial rebalancing

  These are real, well-cited papers:
  1. Kang et al. (2020) "Decoupling Representation and Classifier for Long-Tailed Recognition"
  (ICLR 2020) — showed sqrt-balanced resampling of the classifier head outperforms full
  equalization
  2. Mahajan et al. (2018) "Exploring the Limits of Weakly Supervised Pretraining" (ECCV 2018) —
  square-root frequency sampling beat both uniform and natural frequency on Instagram-1B
  3. Liu et al. (2019) "Large-Scale Long-Tailed Recognition in an Open World" (CVPR 2019) — showed
  aggressive oversampling of tail classes causes overfitting by cycling through same small
  real-image pool

  These papers are about resampling strategies during training, not about synthetic augmentation
  volume specifically. The sqrt formula is principled for how often to show each class, but
  extrapolating it to "how many synthetic images to generate" is my inference, not a direct finding
   from these papers.

  The "5:1 risky for ResNet-50" claim

  This was over-stated by the research agent. Here's what's actually supported:
  - Geirhos et al. (2019) "ImageNet-Trained CNNs Are Biased Towards Texture" — this is real and
  shows ResNet-50 has texture bias. But it says nothing about synthetic:real ratios.
  - Mumuni et al. (2024) survey mentioning 3:1–5:1 crossover — I cannot verify this is a real paper
   or that it contains this specific claim. The research agent may have hallucinated this detail.
  - The "5:1" threshold has no single authoritative source. It's a plausible concern extrapolated
  from multiple findings, not an established number.
  


 Goal: Add caste-aware evaluation + "no match" blind ID option.

 File: scripts/llm_judge.py

 B1. Add "No match" to blind identification

 Current SPECIES_LIST (line 54-61) has 16 Bombus species + "Unknown".

 Changes:
 - Add "No match (not a bumble bee or not identifiable to species)" as an option in the blind ID
 prompt
 - Update the BlindIdentification Pydantic model to handle this response
 - A "No match" result means the image fails — it's not recognizable as any target species

 B2. Add caste-aware evaluation

 The judge currently only uses species_name + morphological_description from SPECIES_MORPHOLOGY.


  ## recommendation for experts

  update interface - add no match/ and add sex recognation
these information also needs to be added into llm as judge.
blind id

## pressing needs
1. dataset build
2. interface
3. diagram -- for presentation and poster
4. writing 


  # Create a named screen session                                                                                          
  screen -S synth                                                                                                          
                                                                                                                           
  # Inside the screen, run your command                                                                                    
  python pipeline/augment/synthetic.py run --species Bombus_ashtoni Bombus_sandersoni Bombus_flavidus --count 100
                                                                                                                           
  # Detach: press Ctrl+A then D                                                                                            
  
  Reconnect anytime with:                                                                                                  
                  
  screen -r synth

  Key commands:                                                                                                            
  - screen -ls — list active sessions
  - screen -r synth — reattach                                                                                             
  - Ctrl+A, D — detach (process keeps running)
  - Ctrl+A, K — kill session when done                                                                                     
                                      
  Your process survives disconnects because screen runs it in a server-side terminal that persists independently of your   
  SSH connection.                                                                                                          
                       